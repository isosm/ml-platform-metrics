version: 2

models:
  - name: metricflow_time_spine
    description: "Daglig time spine — obligatorisk för MetricFlow semantic layer."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
